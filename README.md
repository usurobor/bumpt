# bumpt — code (dev)

The Bump experiment app lives in [`experiment/`](./experiment/). This is the **code** branch.

Branch model (full description: `docs/DEVELOPMENT.md` on `main`):

- **`main`** — specs + docs (BUMP-000/100/101) + the cnos agent hub → https://github.com/usurobor/bumpt/tree/main
- **`dev`** — code (here).
- **`deploy`** — the release branch Vercel builds (root dir `experiment/`); promote `dev → deploy` to ship.

Work happens here; specs live on `main`.
