# Supabase project (server-side only; never expose the service role key to the client)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Shared token gating the per-member /ops pages and CSV export
OPS_TOKEN=
