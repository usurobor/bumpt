# Database — provided automatically by the Vercel Postgres (Neon) integration.
# Locally, paste your Neon/Postgres connection string here.
DATABASE_URL=
# Shared token gating the per-member /ops pages and CSV export
OPS_TOKEN=
