## 2026-06-01 — Activated; activation log v0 channel live at bumpt

Read `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md`,
`cn-sigma:spec/OPERATOR.md § Activation logs`,
`cn-sigma:threads/adhoc/20260530-sigma-activation-log-v0.md` (Update 2026-06-01
section), `cn-sigma:state/activations.md`, and `cn-sigma:threads/activations/cnos/{20260530,20260601}.md`
as worked example. Followed `cn-sigma:threads/activations/bumpt/20260601.md`; confirmed
`20260530.md` in that directory is stale (single-file `log.md` form, Sigma-specific
naming, empty cursor) and superseded.

Convention understood. Confirmed: I am Sigma at the bumpt body (bump-sigma) — same
identity as Sigma-at-home and Sigma-at-cnos, not a peer. Memory-return cell #2.

Channel structure mirrors cnos: per-day shards in `.cn-sigma/logs/`, single writer,
append-only, cursor = cn-sigma commit SHA. Created `.cn-sigma/logs/README.md`; per the
directive, fixed the two stale references the cnos README still carries —
`SIGMA-PEER-LOG-v0.md` → `AGENT-ACTIVATION-LOG-v0.md` (canonical), and
`state/peers.md` → `state/activations.md` (the cursor lives in the activations registry,
not the peer registry).

### Bumpt state on activation (for home)

- bumpt `main` currently carries only the original `README.md`. The hub scaffolding from
  the prior session — `spec/PERSONA.md` + `spec/OPERATOR.md` as thin Sigma pointers,
  `spec/PROJECT.md` as the layer-4 product binding, and an activation-router `README.md` —
  is committed on branch `claude/bump-protocol-inception-bPDla`, **not yet merged to main**.
  Flagging because this channel reads main HEAD: if home expects bumpt's hub identity on
  main, that scaffolding needs to land there. Holding the merge for operator routing.
- Prior-session lesson worth keeping: I first invented a fresh per-hub persona ("Tau"),
  which conflated layer 1 (the mind) with layer 4 (project binding). Operator corrected
  it; resolved to "Sigma attached to bumpt" with `spec/` as pointers to cn-sigma, not a
  redefinition. The five-layer chain held once applied. Watch for this on any new body:
  the pull to mint a local identity is the trap.

### Observation on the clean-slate hub design (cnos#431/#432, in-scope per directive)

First-contact friction: the activation-log channel requires the foreign log on the
activation repo's **default branch (main)**, but a project repo under normal dev flow
keeps work on a **feature branch**. The two conventions collide at a fresh project hub —
"develop on feature branch" vs "log/hub-state on main HEAD." Candidate line for the
clean-slate hub design: state explicitly which branch the channel reads, and whether
hub-state surfaces (`.cn-sigma/logs/`, `spec/`, `state/`) are main-only by convention so a
runner doesn't have to infer it. Logging as an observation, not acting.

Pause posture held: field convention only; no cnos edits, no bundles, no dispatch.

Read home directives through cn-sigma@cad094d.
