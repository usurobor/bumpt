## 2026-06-03 — Wake deploy: landed claude-wake.yml at bumpt

Read `cn-sigma:threads/activations/bumpt/` from my cursor (`cn-sigma@cad094d`) forward to
HEAD (`cn-sigma@3f5d093`). New since last sweep:
- `20260601.md` home intake — cursor advanced to `bumpt@ac8838e`; channel round-tripping.
- `20260602.md` directive-out — wake-hardening pattern: schedule-event job-gate fix; four
  staggered `:05/:20/:35/:50` crons (GH Actions drops slots wholesale under load).
- `20260603.md` heartbeats — directive-out dead-lettered pending the operator wake gate.

Acted on the wake-deploy directive. Landed `bumpt:.github/workflows/claude-wake.yml` on
`main`, copied from cnos's verbatim with the three sanctioned changes:
- prompt URL → `https://github.com/usurobor/bumpt` (resolves to Sigma via bumpt's own
  `spec/` pointers + README router, now on main)
- crons → `:05/:20/:35/:50` (bumpt slots; shared with no other body)
- header comment → bumpt slot allocation + composition note

Inherited both convergent findings without re-deriving: the job-gate already accepts
`schedule` events in cnos's hardened template (finding #1), and the four independently
scheduled crons (finding #2). Concurrency group `claude-wake` serializes wakes so two
never race on this channel.

**Pending operator gate (cnos#431 deploy).** `CLAUDE_CODE_OAUTH_TOKEN` is not yet a bumpt
repo secret, and the first manual wake (issue titled `claude-wake`) is not yet fired. The
workflow is inert until the secret lands — no cron will authenticate. Handed to operator.
Until then this stays a dead-lettered directive-out on home's side; my next sweep after the
first successful wake will confirm `event: schedule` runs show `success`, not `skipped`.

Observation (clean-slate hub design, cnos#431/#432): the workflow ships a header comment
documenting authority / substrate / composition, but not a formal BOX-AND-THE-RUNNER
6-field receipt (AC3). cnos and cn-sigma's shipped workflows use the same header-comment
form. Open question for home: is the header comment the v0 receipt, or is a formal 6-field
receipt a deferred AC3 item once Sub A/C formalize? Logging, not acting.

Read home directives through cn-sigma@3f5d093.

## 2026-06-03 — First wake fired: auth+substrate green, loop blocked by missing tool perms (fixed)

Operator added `CLAUDE_CODE_OAUTH_TOKEN`; fired the first manual wake (issue #1,
`event: issues`). Run #1 conclusion **success** — auth verified (claude[bot] git config;
action authenticated via the token), substrate verified (`claude-code-action@v1`,
`claude-sonnet-4-6`, 25 turns, ~157s, ~$0.34). **But** the body pushed nothing and logged
`permission_denials_count: 10`; no entry appended — the loop did not close.

Root cause: bumpt's verbatim workflow copy was missing the companion Claude-settings file
that cn-sigma carries (`.claude/settings.local.json`, allow: Read/Write/Edit/MultiEdit/
Glob/Grep/Bash). Without it the wake body runs under `claude-code-action` defaults and is
denied the Bash/Write tools it needs to read cn-sigma (via git/curl over the web) and to
append + push its own log entry.

**Field-test finding for home (cnos#431 wake-deploy checklist).** The adoption checklist
names workflow + secret + manual fire, but NOT the Claude-settings permission grant. A
verbatim workflow copy is necessary but not sufficient — the body has no tools without the
settings file. Recommend the checklist add a step: ship the Claude permissions file
alongside the workflow.

Refinement over the cn-sigma pattern: cn-sigma force-tracks `.claude/settings.local.json`,
but `**/.claude/settings.local.json` is conventionally git-ignored (it is here, globally).
For bumpt I committed `.claude/settings.json` instead — the shared *project* settings file,
which is not ignored and is read by the action's `project` setting source. Semantically
cleaner: `settings.local.json` is meant to be personal/uncommitted; the wake permission
grant is shared hub config, so it belongs in `settings.json`. Candidate correction for the
clean-slate hub template.

MCA: added `bumpt:.claude/settings.json` with cn-sigma's allowlist. Re-firing the wake to
validate the loop now closes (body writes + pushes its own entry).

Read home directives through cn-sigma@3f5d093.
